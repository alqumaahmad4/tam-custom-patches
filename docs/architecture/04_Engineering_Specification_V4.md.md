const {
  Document, Packer, Paragraph, TextRun, Table, TableRow, TableCell,
  Header, Footer, AlignmentType, HeadingLevel, BorderStyle, WidthType,
  ShadingType, VerticalAlign, PageNumber, TabStopType, TabStopPosition,
  LevelFormat
} = require('docx');
const fs = require('fs');

const C = {
  ink:'0A0A0A',accent:'1A56DB',accentDk:'1E429F',gold:'B8860B',
  surface:'F8FAFC',border:'E2E8F0',muted:'64748B',white:'FFFFFF',
  dark:'1A1A2E',tagBg:'DBEAFE',sectionBg:'F1F5F9',
  success:'16A34A',error:'DC2626',warn:'D97706',
};
const brd={style:BorderStyle.SINGLE,size:1,color:C.border};
const borders={top:brd,bottom:brd,left:brd,right:brd};
const h1=t=>new Paragraph({heading:HeadingLevel.HEADING_1,children:[new TextRun({text:t,font:'Inter',bold:true,size:38,color:C.white})],shading:{fill:C.dark,type:ShadingType.CLEAR},spacing:{before:480,after:240},indent:{left:360,right:360}});
const h2=t=>new Paragraph({heading:HeadingLevel.HEADING_2,children:[new TextRun({text:t,font:'Inter',bold:true,size:28,color:C.accentDk})],spacing:{before:360,after:160},border:{bottom:{style:BorderStyle.SINGLE,size:4,color:C.accent,space:4}}});
const h3=t=>new Paragraph({heading:HeadingLevel.HEADING_3,children:[new TextRun({text:t,font:'Inter',bold:true,size:24,color:C.ink})],spacing:{before:280,after:120}});
const h4=t=>new Paragraph({heading:HeadingLevel.HEADING_4,children:[new TextRun({text:t,font:'Inter',bold:true,size:21,color:C.muted})],spacing:{before:200,after:80}});
const body=t=>new Paragraph({children:[new TextRun({text:t,font:'Inter',size:22,color:C.ink})],spacing:{after:120}});
const note=(label,t)=>new Paragraph({children:[new TextRun({text:`${label}  `,font:'Inter',bold:true,size:20,color:C.accentDk}),new TextRun({text:t,font:'Inter',size:20,color:C.accentDk})],shading:{fill:C.tagBg,type:ShadingType.CLEAR},indent:{left:360,right:360},spacing:{before:80,after:160}});
const code=t=>new Paragraph({children:[new TextRun({text:t,font:'Courier New',size:18,color:C.accentDk})],shading:{fill:C.sectionBg,type:ShadingType.CLEAR},indent:{left:360,right:360},spacing:{before:40,after:40}});
const sLabel=t=>new Paragraph({children:[new TextRun({text:t.toUpperCase(),font:'Inter',bold:true,size:18,color:C.accent,characterSpacing:80})],spacing:{before:240,after:80}});
const bullet=(t,lv=0,bd=false)=>new Paragraph({numbering:{reference:'bullets',level:lv},children:[new TextRun({text:t,font:'Inter',size:22,bold:bd})],spacing:{after:80}});
const num=(t,lv=0)=>new Paragraph({numbering:{reference:'numbers',level:lv},children:[new TextRun({text:t,font:'Inter',size:22})],spacing:{after:80}});
const spacer=()=>new Paragraph({children:[],spacing:{after:120}});
const divider=()=>new Paragraph({children:[],border:{bottom:{style:BorderStyle.SINGLE,size:4,color:C.border,space:1}},spacing:{before:160,after:160}});
const pb=()=>new Paragraph({children:[new TextRun({break:1})],spacing:{after:0}});
const cell=(t,{w=2400,bg,bd:isBd,color}={})=>new TableCell({borders,width:{size:w,type:WidthType.DXA},shading:bg?{fill:bg,type:ShadingType.CLEAR}:undefined,margins:{top:100,bottom:100,left:140,right:140},verticalAlign:VerticalAlign.CENTER,children:[new Paragraph({children:[new TextRun({text:t,font:'Inter',size:20,bold:isBd||false,color:color||C.ink})]})]});
const hdr=(labels,ws)=>new TableRow({tableHeader:true,children:labels.map((l,i)=>new TableCell({borders,width:{size:ws[i],type:WidthType.DXA},shading:{fill:C.dark,type:ShadingType.CLEAR},margins:{top:100,bottom:100,left:140,right:140},children:[new Paragraph({children:[new TextRun({text:l,font:'Inter',size:20,bold:true,color:C.white})]})]}))});
const tbl=(headers,ws,rows)=>new Table({width:{size:ws.reduce((a,b)=>a+b,0),type:WidthType.DXA},columnWidths:ws,rows:[hdr(headers,ws),...rows.map(r=>new TableRow({children:r.map((c,i)=>cell(c,{w:ws[i]}))}))]});
const kvt=(pairs,w1=2600,w2=6760)=>new Table({width:{size:w1+w2,type:WidthType.DXA},columnWidths:[w1,w2],rows:pairs.map(([k,v])=>new TableRow({children:[cell(k,{w:w1,bd:true,bg:C.sectionBg}),cell(v,{w:w2})]}))});

/* ═══════════════════════════════════════════════════════════════════════════
   SECTION 4 — ANALYTICS & TRACKING PLAN
════════════════════════════════════════════════════════════════════════════ */
const s4=()=>[
  h1('04 · ANALYTICS & TRACKING PLAN'),
  sLabel('Every Event — Exactly Specified'),

  h2('4.1 Tracking Stack'),
  tbl(
    ['Platform','Purpose','Install Method'],
    [2400,3200,3760],
    [
      ['Google Analytics 4 (GA4)','Primary analytics — events, conversions, audience, funnel','gtag.js via Google Tag Manager'],
      ['Google Tag Manager (GTM)','Tag deployment hub — all other tags deployed through GTM','<script> in _document or root layout'],
      ['Microsoft Clarity','Session recordings, heatmaps — qualitative UX data','Script tag via GTM'],
      ['Google Search Console','Organic search performance, indexing, rich results','DNS verification + GSC sitemap submit'],
      ['Meta Pixel (Facebook)','Paid social attribution, retargeting audience creation','Via GTM (fires on quote_submitted)'],
      ['LinkedIn Insight Tag','B2B audience tracking for LinkedIn Ads','Via GTM — fires on all pages'],
      ['Google Ads Conversion','Paid search conversion tracking','Via GTM — fires on quote_submitted'],
    ]
  ),
  note('RULE','All tracking scripts are loaded via Google Tag Manager. No tracking scripts are hardcoded in the Next.js application code except the GTM snippet itself. This allows the marketing team to add/modify tracking without deployments.'),
  spacer(),

  h2('4.2 GA4 Custom Event Definitions'),
  body('All custom events follow the GA4 snake_case naming convention. All monetary values are in USD. All events are fired as client-side GA4 custom events using gtag("event", ...).'),
  spacer(),
  h3('Navigation & Engagement Events'),
  tbl(
    ['Event Name','Trigger','Parameters'],
    [2400,2800,4160],
    [
      ['page_view','Every route change (built-in GA4)','page_title, page_location, page_referrer'],
      ['hero_cta_click','Click on any hero CTA button','slide_number (1|2|3), cta_text, cta_destination'],
      ['nav_link_click','Click on any header navigation link','link_text, link_url, nav_zone (primary|mega|mobile)'],
      ['mega_menu_opened','Mega menu opens (hover or tap)','category (patches|apparel|martial_arts)'],
      ['search_opened','Search overlay opens','trigger_source (header_icon|keyboard_shortcut)'],
      ['search_query','User types in search and gets results','search_term, results_count, result_type (product|blog|faq)'],
      ['search_result_click','User clicks a search result','result_type, result_title, result_url, position'],
      ['faq_expanded','FAQ accordion item opens','question_text (first 60 chars), page_location'],
      ['gallery_opened','Gallery section first comes into view','page_location'],
      ['gallery_image_click','User clicks a gallery image (lightbox opens)','image_index, product_type'],
      ['testimonial_carousel_scroll','User manually scrolls testimonial carousel','direction (prev|next)'],
      ['floating_cta_click','Floating quote button clicked','page_location, scroll_depth_percent'],
    ]
  ),
  spacer(),
  h3('Quote Funnel Events (Primary Conversion Funnel)'),
  tbl(
    ['Event Name','Trigger','Parameters'],
    [2400,2800,4160],
    [
      ['quote_started','Any "Get Free Quote" CTA clicked — user enters /quote','source (hero1|hero2|hero3|header|floating|product_page|industry_page|exit_intent), pre_filled_product'],
      ['quote_step_viewed','Each step renders','step_number (0-6), step_name, product_category, product_type'],
      ['quote_step_completed','User taps "Continue" from a step','step_number, step_name, product_category, quantity (if step 2), color_range (if step 3), size (if step 4)'],
      ['quote_option_selected','User selects any option card in a step','step_number, option_value'],
      ['quote_artwork_uploaded','File upload to R2 completes successfully','file_extension, file_size_kb, upload_duration_ms'],
      ['quote_upload_later','User selects "Upload Later" in step 5','product_category, quantity'],
      ['quote_abandoned','User leaves /quote without submitting (beforeunload + session data)','last_completed_step, product_category, quantity'],
      ['quote_save_initiated','User clicks "Save & Continue Later"','current_step, product_category'],
      ['quote_restored','User returns via saved quote URL','steps_completed_before_save, days_since_save'],
      ['quote_submitted','Quote form submitted successfully','product_category, product_type, quantity, has_artwork, country, how_found, item_count (for multi-product)'],
      ['quote_submit_error','API returns error on submit','error_code, step_number'],
    ]
  ),
  spacer(),
  h3('AI Designer Events'),
  tbl(
    ['Event Name','Trigger','Parameters'],
    [2400,2800,4160],
    [
      ['ai_designer_opened','User navigates to /ai-designer','source (header|hero|product_page|quote_form)'],
      ['ai_prompt_submitted','User clicks "Generate Preview"','prompt_length_chars, product_type, shape, size_inches, color_count'],
      ['ai_preview_generated','Preview image renders successfully','generation_duration_ms, phase (placeholder|ai), seed'],
      ['ai_generation_error','AI generation API returns error','error_code, prompt_length'],
      ['ai_regenerated','User clicks "Regenerate"','version_number (2nd gen, 3rd gen, etc.)'],
      ['ai_design_saved','User saves a design','has_account (bool)'],
      ['ai_design_shared','User copies share link',''],
      ['ai_design_downloaded','User downloads preview image',''],
      ['ai_design_to_quote','User clicks "Request Quote" from AI Designer','has_saved_design, product_type'],
    ]
  ),
  spacer(),
  h3('Product & Content Events'),
  tbl(
    ['Event Name','Trigger','Parameters'],
    [2400,2800,4160],
    [
      ['product_page_viewed','Any product page loads','product_name, product_category, product_type'],
      ['product_cta_click','Quote CTA on product page clicked','product_name, cta_placement (hero|bottom|sticky)'],
      ['contact_form_submitted','Contact form submits successfully','subject_category'],
      ['blog_article_viewed','Blog article page loads','article_title, article_category, author'],
      ['blog_article_scroll_depth','User scrolls to 25/50/75/100% of article','percent, article_slug'],
      ['industry_page_viewed','Industry landing page loads','industry_name'],
      ['newsletter_signup','Newsletter form submitted','source_page'],
    ]
  ),
  spacer(),

  h2('4.3 Google Tag Manager — Trigger & Tag Configuration'),
  tbl(
    ['Tag','Trigger','Platform'],
    [2800,3200,3360],
    [
      ['GA4 Configuration','All Pages — pageview','GA4'],
      ['GA4 Custom Events','Custom Event trigger: name matches regex ^(quote_|ai_|hero_|faq_|gallery_|search_).*','GA4'],
      ['Meta Pixel — PageView','All Pages','Meta Pixel'],
      ['Meta Pixel — Lead','Custom Event: quote_submitted','Meta Pixel'],
      ['LinkedIn Insight Tag','All Pages','LinkedIn'],
      ['Google Ads Conversion — Quote','Custom Event: quote_submitted','Google Ads'],
      ['Microsoft Clarity','DOM Ready — All Pages','Clarity'],
    ]
  ),
  spacer(),

  h2('4.4 GA4 Conversion Events'),
  body('The following events are marked as conversions in the GA4 property:'),
  tbl(
    ['Conversion Event','Value','Notes'],
    [2800,1600,4960],
    [
      ['quote_submitted','$0 (no monetary value in Phase 1)','Primary conversion. Every submission = a qualified lead.'],
      ['contact_form_submitted','$0','Secondary conversion.'],
      ['ai_design_to_quote','$0','Signals high-intent user.'],
      ['newsletter_signup','$0','Marketing list growth metric.'],
    ]
  ),
  spacer(),

  h2('4.5 dataLayer Push Implementation'),
  body('All custom events are pushed to the GTM dataLayer. The Next.js application pushes events via a typed utility function:'),
  code('// lib/analytics/track.ts'),
  code('type EventName = "quote_started" | "quote_step_completed" | "quote_submitted" | ...;'),
  code(''),
  code('export function track(eventName: EventName, params: Record<string, unknown> = {}) {'),
  code('  if (typeof window === "undefined") return;'),
  code('  window.dataLayer = window.dataLayer || [];'),
  code('  window.dataLayer.push({'),
  code('    event: eventName,'),
  code('    ...params,'),
  code('  });'),
  code('}'),
  code(''),
  code('// Usage in components:'),
  code('track("quote_submitted", { product_category: "patches", quantity: 100 });'),
  note('RULE','Never call gtag() directly in application code. Always use the track() utility. This provides type safety and a single place to add logging/debugging.'),
  pb(),

/* ═══════════════════════════════════════════════════════════════════════════
   SECTION 5 — SECURITY SPECIFICATION
════════════════════════════════════════════════════════════════════════════ */
  h1('05 · SECURITY SPECIFICATION'),
  sLabel('Production-Grade Security — Every Layer'),

  h2('5.1 Security Headers'),
  body('All security headers are set in next.config.ts via the headers() configuration. They apply to every response from the Next.js server.'),
  spacer(),
  tbl(
    ['Header','Value','Purpose'],
    [2800,3600,2960],
    [
      ['Content-Security-Policy','See CSP section below','Prevents XSS by whitelisting content sources'],
      ['Strict-Transport-Security','max-age=63072000; includeSubDomains; preload','Forces HTTPS for 2 years'],
      ['X-Frame-Options','DENY','Prevents clickjacking'],
      ['X-Content-Type-Options','nosniff','Prevents MIME-type sniffing'],
      ['Referrer-Policy','strict-origin-when-cross-origin','Controls referrer information in requests'],
      ['Permissions-Policy','camera=(), microphone=(), geolocation=(), interest-cohort=()','Disables unused browser features'],
      ['Cross-Origin-Opener-Policy','same-origin','Prevents cross-origin window attacks'],
      ['Cross-Origin-Resource-Policy','same-site','Prevents cross-origin resource loading'],
    ]
  ),
  spacer(),

  h2('5.2 Content Security Policy (CSP)'),
  body('A strict CSP is defined. Nonces are generated per-request for inline scripts (Next.js handles this). The CSP is defined in middleware.ts and applied as a header.'),
  code("Content-Security-Policy:"),
  code("  default-src 'self';"),
  code("  script-src 'self' 'nonce-{NONCE}' https://www.googletagmanager.com"),
  code("    https://challenges.cloudflare.com https://static.clarity.ms;"),
  code("  style-src 'self' 'unsafe-inline';  /* Tailwind inline styles require this */"),
  code("  img-src 'self' data: blob: https://*.r2.cloudflarestorage.com"),
  code("    https://www.googletagmanager.com;"),
  code("  font-src 'self';"),
  code("  connect-src 'self' https://api.anthropic.com https://*.r2.cloudflarestorage.com"),
  code("    https://www.google-analytics.com https://region1.google-analytics.com;"),
  code("  frame-src https://challenges.cloudflare.com;"),
  code("  object-src 'none';"),
  code("  base-uri 'self';"),
  code("  form-action 'self';"),
  code("  frame-ancestors 'none';"),
  code("  upgrade-insecure-requests;"),
  spacer(),

  h2('5.3 Rate Limiting'),
  body('Rate limiting is enforced in Next.js middleware using Upstash Redis (via Vercel KV). The sliding window algorithm is used.'),
  tbl(
    ['Endpoint','Limit','Window','Response on Exceed'],
    [2800,1200,1200,4160],
    [
      ['POST /api/quote/submit','3 requests','1 minute per IP','429 with Retry-After header'],
      ['POST /api/files/presign','10 requests','1 minute per IP','429'],
      ['POST /api/contact','3 requests','10 minutes per IP','429'],
      ['POST /api/ai/generate (unauth)','5 requests','1 hour per IP','429 with time-until-reset'],
      ['POST /api/ai/generate (auth)','20 requests','1 hour per user','429'],
      ['GET /api/quote/restore','20 requests','1 minute per IP','429'],
      ['All /api/* routes (global)','200 requests','1 minute per IP','429 — protection against scraping'],
      ['Admin /api/admin/*','100 requests','1 minute per IP + must be allowlisted IP','403 if not on allowlist'],
    ]
  ),
  spacer(),

  h2('5.4 Cloudflare Turnstile (Bot Protection)'),
  body('Cloudflare Turnstile is the invisible CAPTCHA used on the quote form and contact form. It requires zero user interaction (no image puzzles) but is highly effective against bots.'),
  kvt([
    ['Client integration','<Turnstile /> React component (react-turnstile package) renders inside the form. On completion, sets the cf-turnstile-response hidden field value.'],
    ['Server verification','Every protected API route verifies the token by POST-ing to https://challenges.cloudflare.com/turnstile/v0/siteverify with the secret key. If verification fails: return 400 TURNSTILE_FAILED.'],
    ['Env vars required','NEXT_PUBLIC_TURNSTILE_SITE_KEY (client-side), TURNSTILE_SECRET_KEY (server-side, never exposed to client)'],
    ['Fallback','If Turnstile JS fails to load (rare), the form is still submittable but the API rejects without the token — users see a "Please refresh and try again" error.'],
  ]),
  spacer(),

  h2('5.5 CORS Configuration'),
  body('CORS is configured in next.config.ts. The API is not a public API — it is consumed only by the same-origin frontend.'),
  code('// next.config.ts'),
  code('async headers() {'),
  code('  return [{'),
  code('    source: "/api/:path*",'),
  code('    headers: ['),
  code('      { key: "Access-Control-Allow-Origin", value: process.env.NEXT_PUBLIC_SITE_URL },'),
  code('      { key: "Access-Control-Allow-Methods", value: "GET, POST, PATCH, DELETE, OPTIONS" },'),
  code('      { key: "Access-Control-Allow-Headers", value: "Content-Type, Authorization" },'),
  code('      { key: "Access-Control-Max-Age", value: "86400" },'),
  code('    ],'),
  code('  }];'),
  code('}'),
  spacer(),

  h2('5.6 File Upload Security'),
  tbl(
    ['Check','Implementation','Failure Response'],
    [2400,3200,3760],
    [
      ['MIME type whitelist','Server validates mimeType field against ALLOWED_MIME_TYPES array before issuing presign URL','400: "File type not supported"'],
      ['File extension whitelist','Server extracts extension from filename and checks against ALLOWED_EXTENSIONS','400: "File type not supported"'],
      ['File size limit','Server checks fileSizeBytes ≤ 52428800 (50MB) before issuing presign URL','400: "File too large. Maximum 50MB."'],
      ['Double extension attack','Regex check: filename must match /^[\\w\\s-]+\\.[a-zA-Z0-9]+$/ — no double extension (e.g. file.pdf.exe)','400: "Invalid filename"'],
      ['Path traversal','Filename is sanitized server-side: replace all / \\ .. with empty string','Sanitized silently'],
      ['Malware scan (Phase 2)','After R2 upload completes, a Cloudflare Worker triggers a scan via VirusTotal API. Infected files: R2 object deleted, quote_files status → virus_detected, admin notified.','File marked as infected, customer notified to re-upload'],
      ['R2 key randomization','The R2 key always includes a UUIDv4 component: quotes/{sessionId}/{uuid}.{ext}. This prevents filename enumeration.','N/A — preventative'],
    ]
  ),
  spacer(),

  h2('5.7 Admin Authentication'),
  kvt([
    ['Auth provider','NextAuth.js v5 with Credentials provider (email + password). No OAuth in Phase 1.'],
    ['Password hashing','bcrypt with work factor 12. Passwords never stored in plaintext.'],
    ['Session type','JWT (JSON Web Token). Signed with HS256. Stored in httpOnly, Secure, SameSite=Strict cookie. 24-hour expiry.'],
    ['Refresh strategy','Session silently refreshed if < 2 hours remaining and user is active. Forces re-login after 24 hours of inactivity.'],
    ['IP allowlist','Admin API routes (/api/admin/*) enforce an IP allowlist at the Next.js middleware level. IPs are stored in ADMIN_ALLOWED_IPS env var (comma-separated).'],
    ['Failed login limit','5 failed attempts per email per 15 minutes triggers a 30-minute lockout. Lockout tracked in Vercel KV.'],
    ['Logout','Deletes the session cookie AND invalidates the JWT in a Redis blocklist. This ensures the token cannot be reused even if intercepted.'],
    ['Password reset','Admin-initiated only (no self-service reset in Phase 1). Super Admin changes passwords via the admin UI.'],
  ]),
  spacer(),

  h2('5.8 RBAC (Role-Based Access Control)'),
  body('RBAC is enforced in middleware.ts for all /admin routes and in each /api/admin route handler.'),
  code('// middleware.ts (simplified)'),
  code('export async function middleware(req: NextRequest) {'),
  code('  const session = await auth(); // NextAuth session'),
  code('  if (req.nextUrl.pathname.startsWith("/admin")) {'),
  code('    if (!session) return NextResponse.redirect("/admin/login");'),
  code('    const requiredRole = getRequiredRole(req.nextUrl.pathname);'),
  code('    if (!hasRole(session.user.role, requiredRole)) {'),
  code('      return NextResponse.json({ error: "Forbidden" }, { status: 403 });'),
  code('    }'),
  code('  }'),
  code('}'),
  spacer(),

  h2('5.9 Data Privacy & Retention'),
  tbl(
    ['Data Type','Storage Location','Retention Period','Deletion Process'],
    [2200,2200,2200,2760],
    [
      ['Quote submissions','PostgreSQL (Phase 2) / Email (Phase 1)','3 years (business records)','Manual delete via admin panel + DB hard-delete'],
      ['Contact form submissions','PostgreSQL / Email','1 year','Manual delete via admin panel'],
      ['Artwork files (R2)','Cloudflare R2','180 days after quote is closed','Automated R2 lifecycle rule'],
      ['AI preview images (R2)','Cloudflare R2','24 hours','R2 lifecycle rule: delete after 1 day'],
      ['Analytics data','GA4 (Google servers)','14 months (GA4 default)','GA4 data deletion request via Google API'],
      ['Session recordings','Microsoft Clarity','30 days','Clarity dashboard data purge'],
      ['Admin session JWTs','Redis (Vercel KV)','24 hours or until logout','Auto-expiry or explicit blocklist entry'],
    ]
  ),
  pb(),

/* ═══════════════════════════════════════════════════════════════════════════
   SECTION 6 — PERFORMANCE BUDGET
════════════════════════════════════════════════════════════════════════════ */
  h1('06 · PERFORMANCE BUDGET'),
  sLabel('Exact Targets — Enforced in CI'),

  h2('6.1 Lighthouse Score Targets'),
  tbl(
    ['Category','Mobile Target','Desktop Target','Blocking Threshold (CI fails if below)'],
    [2000,1600,1600,4200],
    [
      ['Performance','≥ 90','≥ 95','Mobile: 85 / Desktop: 90'],
      ['Accessibility','100','100','100 — zero tolerance'],
      ['Best Practices','≥ 95','≥ 95','90'],
      ['SEO','100','100','100 — zero tolerance'],
    ]
  ),
  spacer(),

  h2('6.2 Core Web Vitals Targets'),
  tbl(
    ['Metric','Target (p75)','Good','Needs Improvement','Poor'],
    [1800,1600,1600,2000,1600, 1760],
    [
      ['LCP (Largest Contentful Paint)','< 1.8s','< 2.5s','2.5s – 4.0s','> 4.0s'],
      ['CLS (Cumulative Layout Shift)','< 0.05','< 0.1','0.1 – 0.25','> 0.25'],
      ['INP (Interaction to Next Paint)','< 150ms','< 200ms','200ms – 500ms','> 500ms'],
      ['TTFB (Time to First Byte)','< 200ms','< 800ms','800ms – 1.8s','> 1.8s'],
      ['FCP (First Contentful Paint)','< 0.9s','< 1.8s','1.8s – 3.0s','> 3.0s'],
    ]
  ),
  spacer(),

  h2('6.3 Bundle Size Budget'),
  tbl(
    ['Bundle','Size Limit (gzipped)','Measurement Tool','Enforcement'],
    [2400,2000,2000,3000],
    [
      ['First JS load (critical)','< 100KB','@next/bundle-analyzer','CI fails if exceeded'],
      ['Total JS (all chunks, homepage)','< 250KB','Bundle analyzer','Warning in CI'],
      ['Hero slide component (lazy)','< 30KB','Bundle analyzer','Warning'],
      ['Quote wizard (lazy CSR)','< 60KB','Bundle analyzer','Warning'],
      ['AI Designer (lazy CSR)','< 40KB','Bundle analyzer','Warning'],
      ['Framer Motion (shared chunk)','< 45KB','Bundle analyzer (currently ~40KB gzip)','Monitor — do not add more Framer imports'],
      ['Tailwind CSS (inline styles)','< 15KB','CSS size from build output','Enforced by Tailwind purge (JIT)'],
    ]
  ),
  spacer(),

  h2('6.4 Image Weight Budget'),
  tbl(
    ['Context','Max Size per Image','Format','Loading Strategy'],
    [2400,1600,1000,4360],
    [
      ['Hero product photography','< 80KB','WebP','preload with fetchpriority=high on active slide only'],
      ['Product card thumbnails','< 25KB','WebP','lazy loading, aspect-ratio: 4/3 reserved'],
      ['Gallery images','< 60KB','WebP','lazy loading, LQIP blur placeholder'],
      ['Blog cover images','< 50KB','WebP','lazy loading'],
      ['Category card images','< 40KB','WebP','lazy loading'],
      ['Author avatars','< 10KB','WebP','lazy loading'],
      ['Open Graph images','< 300KB','PNG','Not loaded by browser — only by crawlers'],
    ]
  ),
  note('RULE','All product photography is processed through a build-time script that generates WebP versions at multiple widths (400, 800, 1200, 1600px). The next/image component selects the correct size via srcset. No image is ever served at a larger size than needed.'),
  spacer(),

  h2('6.5 Font Loading Budget'),
  tbl(
    ['Font','Subset','Formats','Loading Strategy'],
    [1800,1600,1200,4760],
    [
      ['Inter Variable','latin','woff2','next/font/google — self-hosted, inline @font-face, display:swap'],
      ['Inter (weight range)','400-800 only (not all 100-900)','woff2','Weight range reduces subset size by ~40%'],
      ['JetBrains Mono','latin, 400 only','woff2','Loaded only on /ai-designer page — code display font'],
    ]
  ),
  spacer(),

  h2('6.6 Third-Party Script Budget'),
  tbl(
    ['Script','Size (approx)','Load Strategy','Impact on LCP'],
    [2400,1200,2000,3800],
    [
      ['Google Tag Manager','< 30KB','defer + async after load','None (deferred)'],
      ['Cloudflare Turnstile','< 25KB','defer, only on /quote and /contact pages','None (deferred, page-specific)'],
      ['Microsoft Clarity','< 15KB','defer via GTM','None'],
      ['Intercom/Crisp chat','~80KB','Load after 30s idle','None (idle-loaded)'],
      ['Total third-party budget','< 150KB combined','All deferred','Zero LCP impact target'],
    ]
  ),
  spacer(),

  h2('6.7 CDN & Caching Configuration'),
  tbl(
    ['Asset Type','Cache-Control','CDN TTL','Invalidation Strategy'],
    [2400,3000,1400,2600],
    [
      ['JS/CSS bundles (content-hashed)','public, max-age=31536000, immutable','1 year','Automatic — new deploy = new hash = new URL'],
      ['Next.js ISR pages (product pages)','s-maxage=3600, stale-while-revalidate=86400','1 hour','Webhook from CMS triggers /api/revalidate'],
      ['Homepage (ISR)','s-maxage=60, stale-while-revalidate=600','60 seconds','Frequent revalidation for trust stats, etc.'],
      ['Blog articles (ISR)','s-maxage=86400, stale-while-revalidate=604800','24 hours','CMS publish webhook'],
      ['Images from R2 (product photos)','public, max-age=2592000','30 days','Manual purge via Cloudflare dashboard on photo update'],
      ['API responses (quote, contact)','no-store','None','N/A — never cached'],
    ]
  ),
  spacer(),

  h2('6.8 Performance Testing Process'),
  num('Local development: Lighthouse runs via Chrome DevTools on every feature branch before PR submission.'),
  num('CI (GitHub Actions): Lighthouse CI runs on every PR against a production-like build (npm run build && npm start). Fails PR if any target is missed.'),
  num('Post-deploy: Vercel Speed Insights automatically captures real-user performance data (INP, CLS, LCP) from production visitors. Reviewed weekly.'),
  num('Monthly: Run PageSpeed Insights API against the 5 highest-traffic pages. Compare against previous month. Regressions > 5 points trigger an investigation.'),
  num('Quarterly: Full Core Web Vitals audit using CrUX (Chrome User Experience Report) data from Google Search Console. P75 values (not lab values) are the official target.'),
  pb(),

/* ═══════════════════════════════════════════════════════════════════════════
   SECTION 7 — ADVANCED 3D HERO IMPLEMENTATION
════════════════════════════════════════════════════════════════════════════ */
  h1('07 · ADVANCED 3D HERO IMPLEMENTATION'),
  sLabel('Precise Implementation Specification for All Three Hero Slides'),

  h2('7.1 Hero System Architecture'),
  body('The hero system is a single HeroSlider component that manages auto-advance, touch/swipe, keyboard navigation, and renders one of three HeroSlide components at a time. Each HeroSlide contains its own scene-specific 3D component.'),
  spacer(),
  code('// components/hero/HeroSlider.tsx — Component tree'),
  code('<HeroSlider>                          // Manages slide state, timers, controls'),
  code('  <AnimatePresence>'),
  code('    <HeroSlide1 />  |                 // Active slide rendered here'),
  code('    <HeroSlide2 />  |'),
  code('    <HeroSlide3 />'),
  code('  </AnimatePresence>'),
  code('  <SlideControls />                   // Dot navigation, prev/next arrows'),
  code('  <TrustTicker />                     // Scrolling trust signal strip'),
  code('</HeroSlider>'),
  spacer(),
  code('// Each slide:'),
  code('<HeroSlide1>'),
  code('  <HeroContent />                     // Eyebrow, H1, subline, CTAs'),
  code('  <PatchCardScene />                  // The 3D interactive patch card group'),
  code('</HeroSlide1>'),
  spacer(),

  h2('7.2 Hero 1 — Custom Patches: Full Implementation Spec'),

  h3('Scene Storyboard'),
  tbl(
    ['Frame','Time','State'],
    [800,1200,7360],
    [
      ['F1','0ms','Background renders: dark radial gradient + SVG noise texture at 2% opacity. Eyebrow text begins opacity animation.'],
      ['F2','80ms','Headline line 1 enters: translateY 12px→0, opacity 0→1, 500ms ease-out.'],
      ['F3','160ms','Headline line 2 enters with same animation.'],
      ['F4','280ms','Subline enters: translateY 8px→0, opacity 0→1, 400ms ease-out.'],
      ['F5','400ms','CTA buttons enter: translateY 8px→0, opacity 0→1, 400ms spring.'],
      ['F6','500ms','Trust micro-strip enters: opacity 0→1, 300ms ease-out.'],
      ['F7','500ms','Patch Card 1 (Embroidered) enters: translateY 40px→0, scale 0.9→1, opacity 0→1, 600ms spring-gentle.'],
      ['F8','580ms','Patch Card 2 (PVC) enters with same animation.'],
      ['F9','660ms','Cards 3, 4, 5, 6 follow at 80ms intervals.'],
      ['F10','1100ms','All cards visible. Mouse parallax becomes active. Auto-drift begins.'],
    ]
  ),
  spacer(),

  h3('PatchCardScene Component — Full Spec'),
  kvt([
    ['File','components/hero/PatchCardScene.tsx'],
    ['Directive','"use client" — requires window, mousemove, Framer Motion'],
    ['Dependencies','framer-motion, react (useState, useRef, useEffect, useCallback)'],
    ['Container element','<motion.div> with CSS: position:relative, width:560px, height:520px on desktop. perspective:800px on the container.'],
  ]),
  spacer(),
  h3('Card Positioning Data (Exact Values)'),
  tbl(
    ['Card #','Product','translateX','translateY','translateZ','rotateZ','scale'],
    [700,1600,1200,1200,1200,1200,900],
    [
      ['1','Embroidered','-40px','60px','80px','-12deg','1.00'],
      ['2','PVC','20px','0px','60px','-6deg','0.97'],
      ['3','Woven','80px','-40px','40px','0deg','0.94'],
      ['4','Chenille','140px','-30px','30px','+5deg','0.91'],
      ['5','Printed','180px','0px','20px','+8deg','0.88'],
      ['6','Velcro','200px','50px','10px','+14deg','0.85'],
    ]
  ),
  spacer(),
  h3('Individual PatchCard Component Spec'),
  code('// components/hero/PatchCard.tsx'),
  code('interface PatchCardProps {'),
  code('  product: "embroidered"|"pvc"|"woven"|"chenille"|"printed"|"velcro";'),
  code('  position: { x: number; y: number; z: number };'),
  code('  rotation: number;   // rotateZ in degrees'),
  code('  scale: number;'),
  code('  index: number;      // for stagger delay'),
  code('  mouseX: MotionValue<number>; // From useMouse hook on parent'),
  code('  mouseY: MotionValue<number>;'),
  code('}'),
  spacer(),
  h3('Mouse Parallax Implementation'),
  code('// lib/hooks/use-mouse.ts'),
  code('export function useMouse(containerRef: RefObject<HTMLElement>) {'),
  code('  const mouseX = useMotionValue(0); // normalized -1 to +1'),
  code('  const mouseY = useMotionValue(0);'),
  code(''),
  code('  useEffect(() => {'),
  code('    const el = containerRef.current;'),
  code('    if (!el) return;'),
  code('    const handleMove = (e: MouseEvent) => {'),
  code('      const rect = el.getBoundingClientRect();'),
  code('      mouseX.set(((e.clientX - rect.left) / rect.width - 0.5) * 2);'),
  code('      mouseY.set(((e.clientY - rect.top) / rect.height - 0.5) * 2);'),
  code('    };'),
  code('    el.addEventListener("mousemove", handleMove, { passive: true });'),
  code('    return () => el.removeEventListener("mousemove", handleMove);'),
  code('  }, []);'),
  code(''),
  code('  return { mouseX, mouseY };'),
  code('}'),
  spacer(),
  h3('Spring Transform Applied to Card Group'),
  code('// In PatchCardScene.tsx'),
  code('const { mouseX, mouseY } = useMouse(containerRef);'),
  code('const prefersReducedMotion = useReducedMotion();'),
  code(''),
  code('// rotateY: mouse-x maps to ±8 degrees'),
  code('const groupRotateY = useSpring('),
  code('  useTransform(mouseX, [-1, 1], [-8, 8]),'),
  code('  { stiffness: 80, damping: 15, mass: 1.2 }'),
  code(');'),
  code('const groupRotateX = useSpring('),
  code('  useTransform(mouseY, [-1, 1], [4, -4]),'),
  code('  { stiffness: 80, damping: 15, mass: 1.2 }'),
  code(');'),
  code(''),
  code('// Disable mouse parallax when reduced motion is preferred'),
  code('const rotateY = prefersReducedMotion ? 0 : groupRotateY;'),
  code('const rotateX = prefersReducedMotion ? 0 : groupRotateX;'),
  spacer(),
  h3('Auto-Drift Animation (When No Mouse)'),
  code('// Inside PatchCardScene — runs when mouse is not over container'),
  code('const [isHovered, setIsHovered] = useState(false);'),
  code(''),
  code('// Auto-drift: keyframe animation on the group container'),
  code('// Only active when NOT hovered AND NOT reduced motion'),
  code('<motion.div'),
  code('  animate={!isHovered && !prefersReducedMotion ? {'),
  code('    rotateY: [0, 4, 0, -4, 0],'),
  code('    transition: { duration: 8, repeat: Infinity, ease: "easeInOut" }'),
  code('  } : {}}'),
  code('>'),
  spacer(),
  h3('Individual Card Multi-Layer Shadow'),
  code('/* In PatchCard.tsx — box-shadow CSS custom property per card */'),
  code('const shadowValues = {'),
  code('  embroidered: "0 2px 4px rgba(0,0,0,.10), 0 12px 24px rgba(0,0,0,.20), 0 40px 80px rgba(0,0,0,.35)",'),
  code('  pvc:         "0 2px 4px rgba(0,0,0,.12), 0 14px 28px rgba(0,0,0,.22), 0 44px 88px rgba(0,0,0,.38)",'),
  code('  /* ... same pattern for each card type */'),
  code('};'),
  spacer(),
  h3('Card Texture & Visual Treatment'),
  tbl(
    ['Product Card','Background CSS','Border CSS','Texture Layer','Label'],
    [1800,2400,2400,2400,1360],
    [
      ['Embroidered','#1B2A4A (navy)','2.5px gradient border: linear-gradient(135deg,#B8860B,#F5D78B,#B8860B)','SVG feTurbulence noise at 4% opacity inside card','EMBROIDERED'],
      ['PVC','#2D3524 (olive)','2px solid rgba(255,255,255,0.2)','None (smooth PVC surface)','PVC'],
      ['Woven','#FFFFFF','2px solid #E2E8F0','SVG cross-hatch pattern at 6% opacity','WOVEN'],
      ['Chenille','#1A237E (varsity blue)','2px solid #FFD700 (gold)','Raised bump texture SVG at 5%','CHENILLE'],
      ['Printed','Gradient: 135deg,#FF416C,#FF4B2B','None','None (smooth print surface)','PRINTED'],
      ['Velcro','#3E4A37 (ranger green)','2px dashed rgba(255,255,255,0.3)','Fine grid texture at 3% (hook surface)','VELCRO'],
    ]
  ),
  spacer(),
  h3('Framer Motion Variants for Card Entry'),
  code('// lib/animations/variants.ts — shared'),
  code('export const patchCardEntry = (index: number) => ({'),
  code('  hidden: { opacity: 0, y: 40, scale: 0.9 },'),
  code('  visible: {'),
  code('    opacity: 1,'),
  code('    y: 0,'),
  code('    scale: 1,'),
  code('    transition: {'),
  code('      delay: 0.5 + index * 0.08,  // 500ms base + 80ms per card'),
  code('      type: "spring",'),
  code('      stiffness: 120,'),
  code('      damping: 18,'),
  code('    },'),
  code('  },'),
  code('});'),
  spacer(),
  h3('Reduced Motion Fallback — Hero 1'),
  code('// When prefersReducedMotion === true:'),
  code('// 1. Cards appear at final position immediately (no translateY animation)'),
  code('// 2. No auto-drift animation'),
  code('// 3. No mouse parallax (groupRotateX/Y set to 0)'),
  code('// 4. Cards still show their visual design (colors, textures, borders)'),
  code('// 5. Hero still auto-advances on timer (the slider auto-advance is not motion)'),
  code('// 6. Only opacity transition remains: 0→1 in 200ms'),
  spacer(),
  h3('Performance Constraints — Hero 1'),
  tbl(
    ['Constraint','Value','Enforcement'],
    [2400,2400,4560],
    [
      ['CSS transforms only','All positioning uses CSS transform3d — no JS layout','will-change: transform on each card. No JS touches width/height.'],
      ['GPU layer promotion','will-change: transform on card group and each card','Set before animation starts, removed after slide exits via AnimatePresence cleanup'],
      ['No images in Phase 1','All 6 patch cards are CSS + SVG — zero image requests','Verified by network tab: 0 image requests from hero on load'],
      ['Animation frame budget','All spring animations must complete within 16.7ms frames','Spring config validated: gentleSpring never exceeds 60fps frame budget per Framer profiler'],
      ['Passive event listeners','mousemove listener on hero container uses { passive: true }','Prevents blocking scroll behavior'],
    ]
  ),
  spacer(),
  h3('Mobile Version — Hero 1 (< 640px)'),
  tbl(
    ['Element','Desktop Value','Mobile Value'],
    [2000,2800,4560],
    [
      ['Layout','Side-by-side (40/60 split)','Stacked: content top, cards bottom'],
      ['Cards shown','6','3 (Cards 1, 2, 3 only)'],
      ['Card size','240×240px','120×120px'],
      ['Cards visible at once','6 in arc','Fan of 3'],
      ['Mouse parallax','Active','Disabled'],
      ['Auto-drift','±3° rotateY, 8s','±4° rotateY, 5s (tighter range, faster = more engaging on small screen)'],
      ['Gyroscope','Not used','Optional (if DeviceOrientationEvent permission granted)'],
      ['CTA layout','Horizontal (side by side)','Vertical stack, full-width'],
      ['Hero total height','100svh','100svh'],
    ]
  ),
  spacer(),

  h2('7.3 Hero 2 — Custom Apparel: Implementation Spec'),

  h3('ApparelDeckScene Component'),
  kvt([
    ['File','components/hero/ApparelDeckScene.tsx'],
    ['Visual concept','A "fan deck" of 5 garment photography cards. Cards overlap at 40% with increasing z-index.'],
    ['Card dimensions','300px × 360px (portrait 5:6 ratio) — reflects garment proportions. On mobile: 160px × 192px.'],
    ['Card background','White (#FFFFFF) for each card — a clean product photography base. Card itself has --shadow-md at rest.'],
    ['Card border radius','--radius-xl (16px)'],
  ]),
  spacer(),
  h3('Apparel Card Positioning'),
  tbl(
    ['Card','Garment','translateX','translateY','rotateZ','z-index'],
    [800,1400,1200,1200,1200,1000],
    [
      ['1 (front)','Black Hoodie','0px','0px','-4deg','5'],
      ['2','Navy Polo','35px','-18px','-1deg','4'],
      ['3','White T-Shirt','70px','-30px','+2deg','3'],
      ['4','Red Jersey','100px','-22px','+5deg','2'],
      ['5 (back)','Forest Crewneck','125px','-12px','+8deg','1'],
    ]
  ),
  spacer(),
  h3('Fan Interaction — Hover Spread'),
  code('// ApparelDeckScene.tsx'),
  code('const [isSpread, setIsSpread] = useState(false);'),
  code(''),
  code('const spreadPositions = ['),
  code('  { x: 0,   y: 0 },   // Card 1 stays'),
  code('  { x: 80,  y: -10 }, // Card 2 spreads right'),
  code('  { x: 160, y: -15 }, // Card 3'),
  code('  { x: 240, y: -10 }, // Card 4'),
  code('  { x: 320, y: 0 },   // Card 5 fully revealed'),
  code('];'),
  code(''),
  code('// Spring animation for spread'),
  code('// stiffness: 200, damping: 24, mass: 1'),
  code('// 400ms to fully spread, spring-standard'),
  spacer(),
  h3('Background — Hero 2'),
  code('/* Warm off-white gradient — Light section contrast with Hero 1 dark */'),
  code('background: linear-gradient(160deg, #FAFAF8 0%, #F2EDE6 100%);'),
  code('/* Subtle linen texture at 1% opacity */'),
  code('background-image: url("data:image/svg+xml,...feTurbulence baseFrequency=0.65...");'),
  spacer(),

  h2('7.4 Hero 3 — Martial Arts Uniforms: Implementation Spec'),

  h3('MartialArtsScene Component'),
  kvt([
    ['File','components/hero/MartialArtsScene.tsx'],
    ['Visual concept','A single large product photo (the hero gi/uniform) with 3 secondary accent elements: 2 smaller secondary product thumbnails + 1 floating fabric close-up card.'],
    ['Main image dimensions','440px × 560px on desktop. 260px × 330px on tablet. Hidden on mobile (cards only).'],
    ['Main image treatment','Photo of gi on dark background (background already removed in Photoshop). Next/Image with objectFit="contain". No additional styling needed on the image — it renders on the dark hero background.'],
  ]),
  spacer(),
  h3('Scene Element Positions'),
  tbl(
    ['Element','Position (CSS)','Size','Z-index','Animation'],
    [2000,2400,1400,1000,2600],
    [
      ['Main gi photo','right: 60px, top: 80px','440×560px','3','translateX 40px→0 + opacity 0→1, 600ms spring-gentle, 300ms delay'],
      ['Secondary thumbnail 1 (belt)','right: 520px, top: 200px','120×120px','4','Enters 150ms after main image, translateX -20px→0'],
      ['Secondary thumbnail 2 (patch detail)','right: 480px, bottom: 120px','120×120px','4','Enters 250ms after main, translateY 20px→0'],
      ['Fabric close-up card (glass)','left: calc(42% - 40px), bottom: 80px','200×160px','5','Enters last at 400ms delay, scaleIn animation'],
      ['Gold bottom rule','bottom: 0, width: 100%','2px height','2','No animation — present from render'],
    ]
  ),
  spacer(),
  h3('Gold Accent Usage in Hero 3'),
  code('/* Gold bottom border of hero section */'),
  code('background: linear-gradient(90deg, transparent 0%, #B8860B 20%, #D4A020 50%, #B8860B 80%, transparent 100%);'),
  code('height: 2px;'),
  code('width: 100%;'),
  spacer(),
  code('/* Gold underline SVG under "Uniforms & Gear" in headline */'),
  code('/* Animated: strokeDashoffset 100%→0%, 600ms ease-out, 800ms delay */'),
  code('<svg viewBox="0 0 200 4" class="absolute bottom-0 left-0">'),
  code('  <path d="M0 2 Q100 4 200 2" stroke="#B8860B" strokeWidth="2" fill="none" />'),
  code('</svg>'),
  spacer(),
  h3('Light Sweep Animation on Main Gi Image'),
  code('/* A white radial gradient overlay that pulses from 0% to 8% opacity */'),
  code('/* Simulates a spotlight catching the fabric weave */'),
  code('<motion.div'),
  code('  className="absolute inset-0 rounded-lg pointer-events-none"'),
  code('  style={{'),
  code('    background: "radial-gradient(ellipse 60% 40% at 30% 25%, rgba(255,255,255,0.12) 0%, transparent 70%)",'),
  code('  }}'),
  code('  animate={prefersReducedMotion ? {} : {'),
  code('    opacity: [0, 0.8, 0, 0.8, 0],'),
  code('    transition: { duration: 6, repeat: Infinity, ease: "easeInOut", delay: 2 }'),
  code('  }}'),
  code('/>'),
  spacer(),
  h3('Floating Glass Card — Fabric Close-Up'),
  code('// Positioned at bottom-left of the scene'),
  code('// glass-morphism treatment:'),
  code('className={cn('),
  code('  "absolute rounded-xl overflow-hidden",'),
  code('  "bg-white/8 backdrop-blur-[12px] saturate-[180%]",'),
  code('  "border border-white/12",'),
  code('  "shadow-[0_8px_32px_rgba(0,0,0,0.24)]"'),
  code(')}'),
  spacer(),

  h2('7.5 HeroSlider — Transition & Controls Implementation'),

  h3('Slide Transition (AnimatePresence)'),
  code('// HeroSlider.tsx'),
  code('<AnimatePresence mode="wait">'),
  code('  <motion.div'),
  code('    key={currentSlide}'),
  code('    initial={{ opacity: 0 }}'),
  code('    animate={{ opacity: 1 }}'),
  code('    exit={{ opacity: 0 }}'),
  code('    transition={{ duration: 0.6, ease: "easeInOut" }}'),
  code('  >'),
  code('    {currentSlide === 0 && <HeroSlide1 />}'),
  code('    {currentSlide === 1 && <HeroSlide2 />}'),
  code('    {currentSlide === 2 && <HeroSlide3 />}'),
  code('  </motion.div>'),
  code('</AnimatePresence>'),
  note('RULE','mode="wait" in AnimatePresence ensures the exit animation of the current slide completes before the entry animation of the next slide begins. This prevents two slides rendering simultaneously, which would cause a layout collision.'),
  spacer(),

  h3('Auto-Advance Timer Logic'),
  code('useEffect(() => {'),
  code('  if (isPaused || prefersReducedMotion) return;'),
  code('  const timer = setInterval(() => {'),
  code('    setCurrentSlide(prev => (prev + 1) % SLIDE_COUNT);'),
  code('  }, 7000); // 7 second interval'),
  code('  return () => clearInterval(timer);'),
  code('}, [isPaused, prefersReducedMotion]);'),
  code(''),
  code('// Pause on user interaction — restart 10s after last interaction'),
  code('const resetTimer = useCallback(() => {'),
  code('  setIsPaused(true);'),
  code('  const timeout = setTimeout(() => setIsPaused(false), 10000);'),
  code('  return () => clearTimeout(timeout);'),
  code('}, []);'),
  spacer(),

  h3('Touch/Swipe Implementation'),
  code('// Using Framer Motion drag detection on slide container'),
  code('<motion.div'),
  code('  drag="x"'),
  code('  dragConstraints={{ left: 0, right: 0 }}'),
  code('  dragElastic={0.1}'),
  code('  onDragEnd={(_, info) => {'),
  code('    const THRESHOLD = 80;    // px displacement'),
  code('    const VELOCITY  = 300;   // px/s'),
  code('    if (info.offset.x < -THRESHOLD || info.velocity.x < -VELOCITY) {'),
  code('      goToNextSlide();'),
  code('    } else if (info.offset.x > THRESHOLD || info.velocity.x > VELOCITY) {'),
  code('      goToPrevSlide();'),
  code('    }'),
  code('    resetTimer();'),
  code('  }}'),
  code('>'),
  spacer(),

  h3('Dot Navigation Component'),
  code('// SlideControls.tsx'),
  code('interface DotProps { index: number; isActive: boolean; onClick: () => void; }'),
  code(''),
  code('function Dot({ index, isActive, onClick }: DotProps) {'),
  code('  return ('),
  code('    <motion.button'),
  code('      aria-label={`Go to slide ${index + 1}`}'),
  code('      aria-current={isActive ? "true" : "false"}'),
  code('      onClick={onClick}'),
  code('      animate={{'),
  code('        width: isActive ? 24 : 8,  // Active dot: pill (24px). Resting: circle (8px)'),
  code('        backgroundColor: isActive ? "var(--color-accent)" : "rgba(255,255,255,0.4)"'),
  code('      }}'),
  code('      transition={{ duration: 0.3, ease: "easeOut" }}'),
  code('      className="h-2 rounded-full cursor-pointer border-none"'),
  code('    />'),
  code('  );'),
  code('}'),
  spacer(),

  h2('7.6 Acceptance Criteria for Hero Implementation'),
  body('The hero implementation is considered complete when ALL of the following are true:'),
  num('All 3 slides render correctly on mobile (375px), tablet (768px), and desktop (1440px).'),
  num('Auto-advance fires at exactly 7 seconds. Pauses on user interaction. Resumes after 10 seconds.'),
  num('Swipe left/right on mobile changes slides with appropriate threshold.'),
  num('Keyboard: left/right arrow keys change slides when focus is inside the hero.'),
  num('Mouse parallax on Hero 1 activates when mouse is over the card group. Spring physics are visible — NOT instant.'),
  num('All 6 patch cards in Hero 1 have their correct: background color, border treatment, SVG texture layer, product type label.'),
  num('Hero 1 loading sequence: content appears before cards (cards start at 500ms, content starts at 0ms).'),
  num('Hero 2 apparel fan spreads on hover of the card group. Returns to stacked on mouse leave.'),
  num('Hero 3 light sweep animation pulses on the gi image every 6 seconds.'),
  num('Hero 3 gold bottom rule is visible at all viewport widths.'),
  num('prefers-reduced-motion: all card animations disabled. Cards render at final position. Slides still advance manually.'),
  num('Lighthouse Performance score ≥ 90 on mobile with hero fully rendered.'),
  num('First hero slide LCP image (if any) loads in < 1.8s on a simulated 4G connection.'),
  num('No layout shift (CLS = 0) during hero card entrance animations.'),
  num('axe-core: zero accessibility violations in the hero section.'),
  spacer(),
  divider(),
  new Paragraph({children:[new TextRun({text:'TAM CUSTOM PATCHES',font:'Inter',bold:true,size:20,color:C.accent}),...[new TextRun({text:'  ·  Technical Implementation Supplement v4.0  ·  Sections 4–7 Complete',font:'Inter',size:20,color:C.muted})]],alignment:AlignmentType.CENTER,spacing:{before:160,after:80}}),
  new Paragraph({children:[new TextRun({text:'Three-Volume Specification Complete: V1.0 Architecture · V2.0 Systems · V3.0 Visual Design · V4.0 Engineering',font:'Inter',size:18,color:C.muted,italic:true})],alignment:AlignmentType.CENTER,spacing:{before:0,after:0}}),
];

/* ─── assembly ─────────────────────────────────────────────────────────── */
const doc = new Document({
  title:'Tam Custom Patches — Technical Implementation Supplement v4.0 (Part B)',
  styles:{
    default:{document:{run:{font:'Inter',size:22,color:C.ink}}},
    paragraphStyles:[
      {id:'Heading1',name:'Heading 1',basedOn:'Normal',next:'Normal',quickFormat:true,run:{font:'Inter',size:40,bold:true,color:C.white},paragraph:{spacing:{before:480,after:240},outlineLevel:0}},
      {id:'Heading2',name:'Heading 2',basedOn:'Normal',next:'Normal',quickFormat:true,run:{font:'Inter',size:28,bold:true,color:C.accentDk},paragraph:{spacing:{before:360,after:160},outlineLevel:1}},
      {id:'Heading3',name:'Heading 3',basedOn:'Normal',next:'Normal',quickFormat:true,run:{font:'Inter',size:24,bold:true,color:C.ink},paragraph:{spacing:{before:280,after:120},outlineLevel:2}},
      {id:'Heading4',name:'Heading 4',basedOn:'Normal',next:'Normal',quickFormat:true,run:{font:'Inter',size:21,bold:true,color:C.muted},paragraph:{spacing:{before:200,after:80},outlineLevel:3}},
    ],
  },
  numbering:{config:[
    {reference:'bullets',levels:[{level:0,format:LevelFormat.BULLET,text:'•',alignment:AlignmentType.LEFT,style:{paragraph:{indent:{left:720,hanging:360}}}},{level:1,format:LevelFormat.BULLET,text:'◦',alignment:AlignmentType.LEFT,style:{paragraph:{indent:{left:1080,hanging:360}}}}]},
    {reference:'numbers',levels:[{level:0,format:LevelFormat.DECIMAL,text:'%1.',alignment:AlignmentType.LEFT,style:{paragraph:{indent:{left:720,hanging:360}}}}]},
  ]},
  sections:[{
    properties:{page:{size:{width:12240,height:15840},margin:{top:1080,right:1080,bottom:1080,left:1080}}},
    headers:{default:new Header({children:[new Paragraph({children:[new TextRun({text:'Tam Custom Patches  ·  Technical Implementation Supplement v4.0  ·  Part B',font:'Inter',size:18,color:C.muted}),new TextRun({text:'\t',font:'Inter',size:18}),new TextRun({text:'Confidential',font:'Inter',size:18,color:C.muted})],tabStops:[{type:TabStopType.RIGHT,position:TabStopPosition.MAX}],border:{bottom:{style:BorderStyle.SINGLE,size:2,color:C.border,space:4}}})]})},
    footers:{default:new Footer({children:[new Paragraph({children:[new TextRun({text:'Page ',font:'Inter',size:18,color:C.muted}),new TextRun({children:[PageNumber.CURRENT],font:'Inter',size:18,color:C.muted}),new TextRun({text:'\t',font:'Inter',size:18}),new TextRun({text:'tamcustompatches.com',font:'Inter',size:18,color:C.muted})],tabStops:[{type:TabStopType.RIGHT,position:TabStopPosition.MAX}],border:{top:{style:BorderStyle.SINGLE,size:2,color:C.border,space:4}}})]})},
    children:[...s4()],
  }],
});

Packer.toBuffer(doc).then(buf=>{
  fs.writeFileSync('/home/claude/tam-v4/Tam_V4_PartB.docx',buf);
  console.log('✅ Part B written.');
}).catch(e=>{console.error(e);process.exit(1);});